<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/tweet_item"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="8dp"
        android:layout_marginHorizontal="16dp"
        app:cardCornerRadius="8dp">
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/tweet_item_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginVertical="12dp"
            android:layout_marginLeft="12dp">

            <ImageView
                android:id="@+id/profile_icon"
                android:layout_width="32dp"
                android:layout_height="32dp"
                app:layout_constraintTop_toTopOf="@id/tweet_item_container"
                app:layout_constraintLeft_toLeftOf="@id/tweet_item_container" />

            <TextView
                android:id="@+id/profile_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="ryokohbato"
                android:textSize="16dp"
                app:layout_constraintLeft_toRightOf="@id/profile_icon"
                app:layout_constraintTop_toTopOf="@id/tweet_item_container"
                app:layout_constraintBottom_toBottomOf="@id/profile_icon"
                android:layout_marginLeft="8dp"
                />

            <TextView
                android:id="@+id/tweet_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="にゃーん"
                android:textSize="14dp"
                app:layout_constraintTop_toBottomOf="@id/profile_icon"
                app:layout_constraintLeft_toLeftOf="@id/tweet_item_container"
                android:layout_marginTop="8dp" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>